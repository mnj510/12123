# 깃허브에 올려서 링크 만들기

`index.html` 파일 하나를 올리면 `https://아이디.github.io/저장소이름/` 링크가 생깁니다. 코드를 몰라도 됩니다. 웹 화면에서 클릭만 합니다.

## 준비

- GitHub 계정 (github.com — 무료, 이메일만 있으면 됨)
- 만들어 둔 `index.html`

## 1. 저장소 만들기

1. github.com 로그인 → 오른쪽 위 `+` → **New repository**
2. Repository name: 영어 소문자로. 예) `mybook`
3. **Public** 선택 (Private이면 링크가 안 열립니다)
4. **Create repository**

## 2. 파일 올리기

1. 만들어진 저장소 화면에서 **uploading an existing file** 클릭 (또는 `Add file` → `Upload files`)
2. `index.html`을 끌어다 놓기
3. 아래 **Commit changes** 클릭

## 3. Pages 켜기

1. 저장소 위쪽 **Settings**
2. 왼쪽 메뉴 **Pages**
3. Branch: `None` → **`main`** 으로 바꾸고 **Save**

## 4. 링크 확인

1~2분 기다린 뒤 `https://아이디.github.io/mybook/` 을 브라우저에 입력. Settings → Pages 화면 위에도 링크가 뜹니다.

## 5. 고칠 때

`원고.txt` 고치고 `3_책만들기.bat` → 새 `index.html`을 저장소에 같은 방법으로 올리기 (덮어쓰기됨). 링크는 그대로.

## 막히면

| 증상 | 확인 |
|---|---|
| 404 | Pages Branch가 `main`으로 저장됐는지. 파일 이름이 정확히 `index.html`인지 |
| 링크는 되는데 옛날 내용 | 브라우저 새로고침. 1~2분 더 기다리기 |
| Private로 만들었다 | Settings → 맨 아래 Danger Zone → Change visibility → Public |

## 예시

행동게임 전자책: https://okhyun304-dot.github.io/actiongame/ — 저장소 `actiongame`에 `index.html` 하나 올린 것이 전부입니다.
