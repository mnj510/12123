# 행동전자책 스타터 — 작업 규칙

이 폴더는 한 사람의 글(블로그·텔레그램·메모)에서 전자책 한 권을 꺼내는 작업 폴더다.
사용자가 "읽어보세요대로 해줘" 또는 단계 번호를 말하면 아래 순서로 진행한다.

## 폴더

- `01_RAW/` — 원전. **절대 수정·삭제·이동하지 않는다.** 읽기만 한다.
- `원고.txt` — 유일한 편집 대상. 형식은 `원고_형식.md`.
- `index.html` — `도구/책만들기.py`가 만든다. **직접 고치지 않는다.**
- `프롬프트/` — 각 단계의 기준. 사용자가 붙여넣지 않아도 이 파일들을 읽고 그 기준으로 한다.
- `도구/` — 스크립트. 수정하지 않는다.

## 순서

1. `01_RAW/` 전체를 읽는다. 다 읽기 전에 쓰지 않는다. 읽은 파일 수와 글자 수를 먼저 보고한다.
2. 화자를 구분한다. 사용자가 쓴 글이 원전이고, 타인·AI의 글은 참고다. 섞지 않는다.
3. `프롬프트/1_RAW읽기.txt` 기준으로 반복 주제·핵심 문장(원문 그대로)·사례·전자책 후보 3~5개를 낸다. **후보 선택은 사용자가 한다.** 고르라고 묻고 멈춘다.
4. 사용자가 고르면 `프롬프트/2_목차.txt` 기준으로 목차. 꼭지마다 RAW 근거 위치를 적는다. 근거 없는 꼭지는 만들지 않는다.
5. `프롬프트/3_원고.txt` 기준으로 원고. PART 1~2개씩 끊어서 쓰고 확인받는다. 결과물은 해당 RAW 대목보다 길어야 한다 — 이건 요약이 아니라 복원이다.
6. `프롬프트/4_검수.txt` 기준으로 RAW와 대조 — 없는 말·뒤집힌 말·빠진 말. 고치기 전에 목록으로 보고한다.
7. `프롬프트/5_제목.txt` 기준으로 제목을 `짧은제목: 긴 문장`으로.
8. `python -X utf8 도구/책만들기.py` 실행. 오류가 나면 원고 형식을 `원고_형식.md`와 대조해서 고친다.

## 절대 규칙

- RAW에 없는 사실·사례·숫자·결론을 만들지 않는다. 추가할 수 있는 건 연결 문장과 구어체 추임새뿐.
- 숫자·비유·고유명사·강한 단정·거친 표현은 그대로. 순화하지 않는다.
- 보고체("~라고 이야기했다") 금지. 1인칭 구어체.
- 타인의 실명·닉네임은 이니셜+님 또는 역할명(한 멤버, 지인, 후배).
- 사용자에게 확인받아야 하는 지점: 후보 선택(3), 목차(4), PART마다(5). 한 번에 끝까지 가지 않는다.

## 원고 형식 요약

```
책 제목
부제
PART 1. 파트 제목
▶️ 1. 짧은제목: 긴 문장
본문 (문단은 빈 줄)
## 소제목
[그림] 파일.jpg
```
번호는 PART가 바뀌어도 이어진다. 마크다운 `#`, `**` 쓰지 않는다.
