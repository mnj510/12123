# 행동전자책 스타터

내 글을 모아서 전자책 한 권을 만드는 폴더입니다. 순서대로 하면 됩니다.

## 0. 처음 한 번 — `_준비.bat` 더블클릭

파이썬이 있는지 확인하고 필요한 부품을 설치합니다. 파이썬이 없으면 안내가 뜹니다.

## 1. 모으기 — `01_RAW/` 채우기

| 어디서 | 어떻게 | 어디로 |
|---|---|---|
| 네이버 블로그 | `1_블로그수집.bat` → 아이디 입력 | `01_RAW/블로그/` 에 글마다 md |
| 텔레그램 | 텔레그램 데스크톱 → 방 → ⋮ → Export chat history → JSON → `2_텔레그램변환.bat` | `01_RAW/텔레그램/텔레그램.md` |
| 메모·복기·카톡 | 그냥 파일을 넣는다 | `01_RAW/메모/` |

원칙: **01_RAW는 원전입니다. 여기 파일은 절대 고치지 않습니다.** 고칠 건 원고입니다.

## 2. 꺼내기 — Claude(또는 GPT)에게 `01_RAW`를 읽히기

**Claude Code가 있으면** — 이 폴더를 열고 "읽어보세요대로 해줘"라고 하면 됩니다. 폴더 안 `CLAUDE.md`를 자동으로 읽어서 아래 순서를 알고 있습니다.

**Claude.ai / GPT 웹이면** — `01_RAW` 파일들을 채팅에 첨부하고 `프롬프트/0_시작.txt`를 먼저 붙여넣습니다. 그다음 카드를 순서대로 붙여넣습니다.

**맥이면** — bat 대신 터미널에서 `python3 도구/책만들기.py` 처럼 실행합니다.

| 카드 | 시키는 일 | 결과 |
|---|---|---|
| `1_RAW읽기.txt` | 전체를 읽고 반복 주제·중요한 생각·사례·전자책 후보를 뽑기 | 후보 목록 |
| `2_목차.txt` | 후보 하나를 골라 PART/꼭지 목차 | `목차.txt` |
| `3_원고.txt` | 목차대로 원고. 요약이 아니라 복원 | `원고.txt` |
| `4_검수.txt` | 원고를 RAW와 대조 — 없는 말·뒤집힌 말·빠진 말 | 수정 목록 |
| `5_제목.txt` | 꼭지 제목을 `짧은제목: 긴문장`으로 | 완성 원고 |

후보 중에 **무엇을 책으로 만들지는 사람이 고릅니다.** AI는 후보만 냅니다.

## 3. 만들기 — `3_책만들기.bat` 더블클릭

`원고.txt` → `index.html`. 더블클릭하면 책이 열립니다. 폰으로 보내도 그대로 열립니다.

- 표지: 폴더에 `표지.jpg`를 넣으면 사진 표지, 없으면 검정 텍스트 표지
- 프롤로그: `프롤로그.md`를 넣으면 차례 앞에 들어감
- 그림: 원고에 `[그림] 파일명.jpg` 쓰고 `그림/` 폴더에 파일

원고 형식은 `원고_형식.md`. 지금 들어 있는 `원고.txt`는 예시라서 지우고 내 것으로 바꾸면 됩니다.

## 4. 올리기 — `깃허브_올리기.md`

GitHub에 `index.html` 하나 올리면 링크가 생깁니다. 5단계.

## 고칠 때

**`원고.txt`만 고치고 `3_책만들기.bat`를 다시 누릅니다.** `index.html`을 직접 고치면 다음에 만들 때 날아갑니다.

## 어디서 끝나도 됩니다

| 여기까지 | 남는 것 |
|---|---|
| 1 | 내 글이 처음으로 AI가 읽을 수 있는 파일로 모였다 |
| 2 | 전자책 후보 목록 + 목차 |
| 3 | 책 파일 |
| 4 | 링크 |

1까지만 가도 다음 주에 이어서 하면 됩니다.
